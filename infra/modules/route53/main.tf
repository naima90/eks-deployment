# Route53 + TLS working

# Create Route53 hosted zone (or use existing)

# external-dns updates an A/CNAME to the ingress public LB

# cert-manager issues a Let’s Encrypt cert

# App live at: https://eks.<domain> (or eks.labs.<domain>)